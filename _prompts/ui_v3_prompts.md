# Mini App v3 — промпты код-агенту (порядок: A → отчёт → B → отчёт → C)

Проверено по факту кода: экраны `screenDashboard/Tasks/NewTask/TaskDetail/Hours/NewHours/Reports/Team/Clients/Inventory/Payments/Profile`;
функции `renderDashboard:577`, `renderTasks:588`, `loadTeam:1137`, `loadClients:1203`, `loadInventory:1265`, `loadPayments:1328`.
PATCH поддерживают `users/{id}` (hourly_rate, is_manager, is_active), `clients/{id}`, `inventory/{id}` (держатель).
Все три промпта трогают `webapp/app.js` → отдавать строго последовательно.

Общее для всех: не трогать `app/routers/miniapp.py` и порядок роутеров в `app/main.py`;
деньги — Decimal/строками; все тексты через `I18N` (ru/uk/es); ни одного `alert()`, только `toast()`.

---

## ПРОМПТ A — навигация, шапка, дашборды по ролям, карточка задания

```
Задача: переделать навигацию, шапку и главный экран Mini App по ролям.

Прочитай: D:\TimeControl/webapp/index.html, webapp/app.js,
webapp/styles.css, app/schemas/__init__.py (TaskShortOut, TaskOut, UserOut),
app/routers/tasks.py, app/routers/miniapp.py.

1) Шапка. Убери текст "AgroManager". Сделай .app-header:
   слева лого-марка 32x32 (радиус 10px, фон --accent, буква "A"
   цветом --accent-ink, 700), справа две строки:
   #headerOrg (16px/700) и #headerMeta (12px muted).
   #headerMeta всегда = t('roleManager'|'roleWorker') + ' · ' + сегодняшняя
   дата в локали (ru: "11 сентября", uk/es аналогично, через
   toLocaleDateString(state.lang)). #headerOrg = t('appOrg')
   (ru "Агрофирма", uk/es перевести). Добавь ключи appOrg, roleManager,
   roleWorker в I18N (ru/uk/es), ничего не хардкодь.

2) Нижняя навигация. Сейчас .bottom-nav есть только в CSS, в index.html
   его нет. Добавь разметку <nav class="bottom-nav" id="bottomNav">
   с 5 элементами (кнопки с data-nav и data-i18n):
   Руководитель: dashboard "Главная", tasks "Задания", reports "Отчёты",
     team "Команда", profile "Профиль"
   Сотрудник:   dashboard "Главная", tasks "Мои задания",
     reports "Отчёты", inventory "Инвентарь", profile "Профиль"
   Кнопка "ещё"/бургер из старой навигации (app-nav, more-menu)
   удаляется, остальные пункты (клиенты, инвентарь, выплаты) у
   руководителя доступны с главной — см. п.4.
   Активный элемент: класс is-active, цвет --accent.
   Функция showScreen(name) должна подсвечивать соответствующий
   пункт навигации. Иконки — инлайн SVG 24x24, stroke currentColor,
   stroke-width 1.8 (никаких эмодзи вместо иконок).

3) Дашборд РУКОВОДИТЕЛЯ, новый renderDashboard при me.is_manager:
   - карточка .stat-row: активных заданий / часов за неделю /
     заданий за месяц.
   - заголовок "Активные задания", ниже список .card--tap
     (не более 5): строка 1 = название задания, строка 2 =
     клиент · локация, строка 3 = список имён сотрудников через "·",
     справа бейдж статуса и часы за сегодня.
     Данные: GET /api/tasks (TaskShortOut) + GET /api/clients для
     имён клиентов + GET /api/users для имён. Сверь поля по схемам,
     не выдумывай. Тап по карточке открывает карточку задания (п.5).
   - кнопки-действия сеткой 2x2 (.btn--primary / .btn--ghost):
     "Новое задание", "Заказчики", "Инвентарь", "Выплаты".
   - В САМОМ НИЗУ, мелко, сворачиваемая строка
     "Мои часы: N ч · начислено N" (данные GET /api/work_entries
     с фильтром по себе, только если N > 0, иначе строку скрыть).

4) Дашборд СОТРУДНИКА (me.is_manager === false):
   - .stat-row: часы сегодня, начислено за неделю.
   - "Мои задания сегодня" — карточки, тап открывает карточку задания.
   - блок "Мои часы за неделю" — список записей (задание, дата, часы).
   Никаких кнопок руководителя.

5) Карточка задания = bottom sheet, не отдельный экран.
   Добавь в index.html <div class="sheet" id="sheetTask" hidden> с
   .sheet__backdrop и .sheet__handle, внутри: #sheetTaskTitle,
   #sheetTaskSub (клиент · локация), #sheetTaskBadge, список
   "Кто работает" (имя · часы за сегодня), сумма часов и начислено,
   и кнопки: "Ввести часы" (.btn--primary, оставь id, задача B его использует),
   "Редактировать" (только руководитель), "Закрыть" (.btn--ghost).
   Данные: GET /api/tasks/{id}. Функция openTaskSheet(id), закрытие
   по тапу на backdrop и по Escape. Экран screenTaskDetail оставь,
   но с дашбордов ведёт лист.

Ограничения: не трогай app/routers/miniapp.py, порядок роутеров в
app/main.py, логику api()/toast() из прошлой задачи, работу с
initData. Все тексты — только через I18N (ru/uk/es), ключи с
осмысленными именами. После правок проверь синтаксис app.js (node --check).

Отчёт: списком — какие экраны/функции заменил, какие id добавил в
index.html, какие ключи i18n добавил, какие эндпоинты вызвал в каждом
блоке дашборда.
```

---

## ПРОМПТ B — часы из карточки, удаление раздела «Часы», шиты, picker в инвентаре

```
Задача: убрать раздел "Часы" из меню и перевести ввод часов в карточку
задания; сделать списки сотрудников и заказчиков редактируемыми через
bottom sheet; в инвентаре выбирать сотрудника из списка, а не вводить ID.

Прочитай: D:\TimeControl/webapp/app.js, webapp/index.html,
webapp/styles.css, app/schemas/work_entries.py,
app/routers/work_entries.py, app/routers/users.py,
app/routers/clients.py, app/routers/inventory.py, app/models.py.

1) Убрать раздел "Часы": из bottom-nav и с дашбордов убери пункт Hours.
   Экраны screenHours/screenNewHours и функции renderHours/loadHours
   удали вместе с разметкой. Ввод часов теперь только из карточки
   задания (sheetTask, кнопка "Ввести часы").

2) Лист ввода часов #sheetHours:
   - шапка: название задания.
   - список назначенных сотрудников: строки с именем и числовым
     полем часов (inputmode="decimal", step 0.5), по умолчанию 0
     (пусто). Над списком переключатель .chip--on "Всем одинаково" —
     при включении поле "Часы" одно, значение применяется ко всем
     выбранным.
   - поле "Комментарий" (необязательно).
   - кнопки "Сохранить" (.btn--primary) и "Завершить задание"
     (.btn--ghost, только для учётчика/руководителя).
   Сохранение: POST /api/work_entries/group (GroupEntryResult) —
   сверь схему GroupEntryCreate по app/schemas/work_entries.py
   и app/services/work_entries.py, поля не выдумывай.
   После успеха: тост, закрыть лист, обновить дашборд и карточку.
   "Завершить задание" = сохранить часы, затем PATCH /api/tasks/{id}
   со status done (сверь допустимые значения TaskStatus в
   app/models.py), и тост "Задание завершено".
   Если POST вернул skipped_members — покажи, кого пропустили.

3) Правка часов: в списке записей задания тап по записи открывает
   тот же лист в режиме правки — поле часов + ОБЯЗАТЕЛЬНОЕ поле
   причины (кнопка "Сохранить" неактивна, пока причина пустая),
   PATCH /api/work_entries/{entry_id}. Существующую историю
   изменений (GET /api/work_entries/{id}/history) покажи ссылкой
   "История изменений" — открыть список с датой, пользователем,
   старой/новой величиной и причиной.

4) Шит сотрудника (руководитель): тап по строке в "Команда" открывает
   #sheetUser: поля "Имя" и "Ставка" (inputmode="decimal"),
   переключатель "Руководитель" (is_manager), "Активен" (is_active),
   кнопки "Сохранить" (PATCH /api/users/{id} с hourly_rate как строкой
   без валютного символа) и "Удалить/деактивировать" (.btn--danger,
   DELETE /api/users/{id}). Кнопку "Ставка" из строки списка УДАЛИ —
   строка целиком тапается.

5) Шит заказчика: тап по строке в "Заказчики" открывает #sheetClient:
   "Название", "Заметки" (PATCH /api/clients/{id}), ниже список
   локаций клиента с иконкой-карандашом на каждой
   (PATCH /api/clients/{id}/locations/{location_id}) и кнопка
   "Добавить локацию" (POST /api/clients/{id}/locations: name, address,
   map_url). Кнопку "Локация" из строки списка УДАЛИ.

6) Инвентарь: вместо ввода ID сотрудника — поле-селектор, открывающий
   список сотрудников (GET /api/users, только is_active), выбор
   проставляет holder_id в PATCH /api/inventory/{id}. То же для выдачи
   новой позиции (POST /api/inventory). Если держатель — ячейка уже
   занята, показывай тост с текстом ошибки сервера.

Ограничения те же: miniapp.py и порядок роутеров не трогать,
деньги строками, ни одного alert(), всё через toast(),
тексты только через I18N ru/uk/es.

Отчёт: что удалил (функции/экраны/кнопки), какие id листов добавил,
какие эндпоинты вызываешь на сохранение часов и правку, какие ключи
i18n добавил.
```

---

## ПРОМПТ C — раздельные отчёты и профиль

```
Задача: сделать разные экраны отчётов для руководителя и сотрудника,
добавить в профиль ставку и закреплённый инвентарь.

Прочитай: D:\TimeControl/webapp/app.js, webapp/index.html,
app/routers/reports.py, app/services/payroll.py,
app/routers/payroll.py, app/routers/inventory.py.

1) Фильтр периода общий для обеих ролей: .chip--on "Сегодня / Неделя /
   Месяц", по умолчанию Неделя. Даты считай на клиенте в YYYY-MM-DD.

2) Отчёт СОТРУДНИКА (is_manager === false):
   - .stat-row: часов всего, начислено (руб).
   - список "Мои задания": название, дата, часы по этой записи,
     начислено (часы × rate_snapshot). Тап — открыть карточку задания.
   - разворачиваемая строка "Все записи за период" со списком:
     дата, задание, часы, начислено. Служит самопроверке — если
     запись в приложении отсутствует, человек это видит.
   Данные: GET /api/reports/user/{me.id} (сверь параметры и поля
   ответа по app/routers/reports.py) либо GET /api/work_entries
   с фильтром периода, если отчёт не отдаёт нужных полей.

3) Отчёт РУКОВОДИТЕЛЯ:
   - .stat-row: заданий всего, из них завершено, часов всего.
   - "ФОТ за период" = сумма начислений (можно через
     GET /api/payroll/preview, сверь параметры).
   - "По заказчикам": клиент, заданий, часов — сортировка по часам.
   - "По сотрудникам": имя, часов, начислено — сортировка по часам.
   - В самом низу, мелко, сворачиваемая "Мои часы за период"
     (его личная статистика, по умолчанию свёрнута).
   Свои личные часы НЕ выноси в топ.

4) Профиль:
   - ФИО, телефон, язык (существующее оставь), плюс:
     "Ставка: N ₽/ч" (hourly_rate из GET /api/users/me),
     для руководителя — "Роль: Руководитель".
   - "Мой инвентарь": список позиций, где holder_id === me.id
     (GET /api/inventory, фильтруй на клиенте), с датой выдачи;
     пусто — .empty с текстом "Инвентаря нет".
   - Переключение языка ru/uk/es оставь, после смены —
     перерисовать текущий экран без перезагрузки, «голых» ключей
     быть не должно: пройди по всем t('...') в app.js и убедись,
     что каждый ключ есть во всех трёх языках.

Ограничения: те же. Деньги выводи числом с двумя знаками,
символ валюты только в подписи, не внутри расчётов.
Никаких alert().

Отчёт: какие эндпоинты использовал для каждого блока, какие ключи
i18n добавил, какие расхождения нашёл между фронтом и API.
```
